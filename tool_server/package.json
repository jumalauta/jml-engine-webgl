{
  "name": "tool_server",
  "version": "1.0.0",
  "description": "JML Engine tool server",
  "main": "src/server.js",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --experimental-vm-modules ./node_modules/.bin/jest --config ./jest.config.mjs",
    "test:watch": "node --experimental-vm-modules ./node_modules/.bin/jest --watch --config ./jest.config.mjs",
    "test:coverage": "node --experimental-vm-modules ./node_modules/.bin/jest --coverage --config ./jest.config.mjs"
  },
  "author": "",
  "license": "Zlib",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "diff": "^8.0.3",
    "pino": "^10.3.1",
    "uuid": "^13.0.0",
    "ws": "^8.19.0"
  },
  "optionalDependencies": {
    "bufferutil": "^4.1.0"
  },
  "devDependencies": {
    "jest": "^30.2.0"
  }
}
